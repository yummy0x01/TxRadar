# Curated lifecycle logs

The graded lifecycle log (≥10 real bundle submissions, ≥2 failures) is captured
here after the run campaign. Live runs write to `logs/lifecycle.jsonl`
(gitignored); curated, explorer-verified runs are committed under this folder.
